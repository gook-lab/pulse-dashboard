# PULSE — 기능명세서

> 지수 통합 시황 대시보드. React 18 + Vite + TypeScript.
> 시안: `PULSE 시황 대시보드 설계/MarketDesk.dc.html` · 핸드오프 코드: `handoff/`
> 최종 갱신: 2026-07-24

---

## 0. 공통 (전 화면 적용)

### 0.1 스택 / 규약
| 항목 | 결정 |
|---|---|
| 프레임워크 | React 18 + Vite + TypeScript |
| 상태관리 | zustand (전역 스토어 1개, slice로 분리) |
| 스타일 | CSS Module 또는 Tailwind (인라인 스타일 금지) |
| 폰트 | Pretendard(UI) · JetBrains Mono(숫자·티커) |
| 실시간 | `handoff/useKisSocket.ts` — WebSocket 훅으로 추상화 |
| 반응형 | 데스크탑 우선(min-width 1280). 좁은 화면은 가로 스크롤 허용 |

### 0.2 색상 토큰
```
배경 #0A0D14 · 패널 #0F131C · 보더 #1B2130
텍스트 #EDF0F6(주) · #8A93A6(보조) · #5E6879(약)
브랜드 #7C6CFF
```
**등락 색상 = 국제식 확정.** 상승 `#16C784` / 하락 `#EA3943`.
- 전역 토글 구조는 유지(`global` / `korea`)하되 **기본값 = global**.
- `korea` 모드(상승 #F6465D·하락 #4C82FB)는 설정에서 전환. 색상은 `up`/`down` prop으로 전 컴포넌트 주입(호가·체결·차트·리포트 포함).

### 0.3 공통 레이아웃
- **상단 앱바**: 브랜드 · 탭 네비(대시보드/종목상세/뉴스/포트폴리오/리서치) · 뉴욕·서울 2개 시계 · WebSocket 연결상태 인디케이터 · 종목 검색
- **하단 티커테이프**: 주요 지수·환율 가로 스크롤(무한 루프)
- **탭 전환**: 클라이언트 라우팅. 대시보드 관심종목 클릭 → 종목상세로 이동(선택 종목 컨텍스트 전달)

### 0.4 데이터 소스 & 오픈 API 매핑 (M0 착수 목록)

블록별 소스 · 오픈 API 여부 · 비용. **"공식 오픈 API가 없어 못 만드는 블록은 없음"** — F&G·맵만 자체 산출, 재료는 전부 API로 확보.

| 블록 | 소스 | 오픈 API | 비용 | 갱신 |
|---|---|---|---|---|
| 국내/해외 실시간 체결·호가·계좌·주문 | **한국투자증권 KIS OpenAPI** (REST+WebSocket) | ✅ 공식 | 무료 (해외 실시간만 신청/조건) | tick |
| 국내 지수·재무(PER/PBR/EPS/배당) | KIS REST | ✅ 공식 | 무료 | 15s~1분 / 종목 진입 시 |
| 미국 지수·해외 시세 | Finnhub / Alpha Vantage / Polygon / Twelve Data | ✅ 공식 | 무료 티어(rate limit) | 15s~1분 폴링 |
| 환율·금리·경제지표 | **한국은행 ECOS**, 미국 **FRED** | ✅ 공식 | 무료 (강력 추천) | 1시간~1일 |
| **서울 전세(구별 지도)** | **data.go.kr 아파트 전월세 실거래가** (`RTMSDataSvcAptRent`) | ✅ 공식 | 무료 | 12h 캐시 |
| 비트코인 | **업비트 공개 API** (`api.upbit.com/v1/ticker`) | ✅ 공식 | 무료·키불필요 | 30초 캐시 |
| 유가·금 | FRED, Alpha Vantage(commodities) | ✅ 공식 | 무료 티어 | 1시간 |
| 뉴스 + 감성 | Alpha Vantage News&Sentiment, Finnhub, Marketaux / 국내 네이버 뉴스 검색 API | ✅ 공식 | 무료 티어 | 수집 주기 |
| **증시 Fear & Greed** | CNN 비공식 JSON (`production.dataviz.cnn.io/index/fearandgreed/graphdata`) | 비공식 | 무료 | 30분 캐시 |
| **크립토 Fear & Greed** | **alternative.me** (`api.alternative.me/fng/`) | ✅ 공식 | 무료 (⚠️ **출처 표기 필수**) | 30분 캐시 |
| **마켓 맵(히트맵)** | Finviz — 공식 API **없음**(스크래핑=TOS 위반) | ❌ | **자체 트리맵**(구현 완료) | 1분 |
| AI 투자의견/리포트 | 자체 LLM 파이프라인 | — | 모델 비용 | 캐싱(일 단위) |

**핵심 결론**
- **한국 커버리지 매우 유리** — KIS(시세) + 한국은행 ECOS(금리·환율) + data.go.kr(전세·부동산)로 전부 **무료 공식 API**.
- **미국은 무료 티어 조합**(Finnhub·Alpha Vantage·FRED) → rate limit 때문에 **백엔드 캐시 필수**(M0).
- **증시 F&G** → CNN 비공식 JSON을 **백엔드 프록시로 소비**(자체 산출 불필요). ⚠️ **헤더 교훈**: User-Agent만이면 **418(봇 차단)**, `Referer: https://www.cnn.com/markets/fear-and-greed` + `Origin` + 정식 UA를 넣어야 200. → `server/index.mjs`에 반영·검증 완료.
- **크립토 F&G** → alternative.me 공식 무료 API. **표시 옆 출처 표기 필수**(대시보드 F&G 카드에 `alternative.me` 명시).
- **Finviz 맵만 공식 API 부재** → squarified 트리맵으로 **자체 구현 완료**(구성종목 시총·등락은 지수 API로).

**M0 진행 상태** — 대부분 실연동 완료:
- ✅ 증시 F&G(CNN) · 크립토 F&G(alternative.me) · BTC(업비트) — 키 불필요
- ✅ 매크로: 기준금리·국고채3년·원/달러·원/엔 (**ECOS KeyStatisticList** 100대 지표, 1콜) + 미10Y·WTI(FRED)
- ✅ 미국 지수 3종 = ETF 프록시 SPY/QQQ/DIA(Finnhub) — ⚠️ 무료는 지수레벨 미제공 → ETF, 라벨 명시
- ✅ **KOSPI·KOSDAQ** + **관심종목 국내**(KIS 모의) + **관심종목 미국**(Finnhub)
- ✅ **서울 전세 데이터센터** — data.go.kr 25구 실거래, d3-geo 코로플레스 + KPI + 정렬 랭킹표 + 시장포지셔닝 사분면 + 보증금 분포 히스토그램
- ✅ **뉴스+감성** — Alpha Vantage NEWS_SENTIMENT, **1시간 배치 + 수동 갱신 버튼** (일 25회 → 1h 캐시로 ≤24/일)

**교훈(명세 고정)**:
- CNN·data.go.kr 모두 **User-Agent 없으면 차단**(418/400). data.go.kr는 **50콜 동시 시 throttle** → 동시성 5+재시도.
- **KIS 토큰 1분 1회 발급 제한** + React StrictMode 이중호출 → 토큰/캐시 **in-flight 중복 제거** 필수.
- Finnhub·KIS 무료/모의는 지수레벨·계좌주문 한계 → ETF 프록시·모의계좌로 우회.

인프라: `server/index.mjs`(env·TTL캐시·헤더위장·동시성풀·in-flight dedup·KIS 토큰캐시) + Vite `/api` 프록시 + `httpApi` strangler + `public/maps/seoul-simple.json`.

- ✅ VIX(FRED VIXCLS) · 금(Finnhub GLD ETF)
- ✅ **히트맵 개별종목 실시세** — 미국(Finnhub) + KOSPI(KIS 이름→코드), 3개 마켓(S&P500·나스닥100·코스피) 토글
- ✅ **AI 종합 투자의견** — 규칙 기반 실산출(F&G+지수 모멘텀+뉴스 감성). LLM 키 주면 교체 가능
- ✅ **KIS 실시간 웹소켓** — approval_key 백엔드 발급 + ws 연결·구독(체결 H0STCNT0/호가 H0STASP0), tick flash. in-flight/PINGPONG 버그 수정. 장중 실데이터, 장마감 폴백
- ✅ **캔들차트** — KIS 일/주/월봉 실데이터(inquire-daily-itemchartprice), OHLC+거래량 SVG
- ✅ **리서치 리포트** — 현재가·상승여력 실시세 갱신(재무는 목)
- ✅ **디자인 시스템** — Tailwind+framer-motion, 공통 컴포넌트(Button/Segmented/Loading/Skeleton/Badge/EmptyState/Modal/ConfirmDialog), react-hot-toast(viviane 패턴)

**입력 대기(외부 값 필요)**: 포트폴리오 실계좌(KIS_ACCOUNT_NO 필요) · 리서치 재무지표 PER/PBR(유료/별도 소스) · LLM 리포트/의견(모델·API 키).

### 0.5 백엔드 프록시 (필수)
프론트가 외부 API를 직접 호출하지 않는다. 백엔드가 담당:
1. **한투 인증** — `appkey`/`appsecret` 보관, `POST /oauth2/Approval`로 `approval_key` 발급 → 만료 짧은 키만 브라우저로 전달. REST용 access token도 백엔드 관리.
2. **CORS·레이트리밋 흡수** — CNN F&G, 뉴스 API 등 프록시 + 캐시.
3. **히스토리 저장** — 지수·뉴스·AI의견 스냅샷을 DB에 적재(→ 뉴스 히스토리, 의견 아카이브의 원천).

### 0.6 실시간 훅 (handoff/useKisSocket.ts)
- 소켓 1개 공유(중복 구독 방지), 지수 백오프 재접속(최대 30s), PINGPONG 반사, 재접속 시 자동 재구독.
- TR_ID: 국내 체결 `H0STCNT0`·호가 `H0STASP0` / 해외 체결 `HDFSCNT0`·호가 `HDFSASP0`.
- 해외 tr_key는 거래소 프리픽스 — 현재 `toUsKey()`는 `DNAS`(나스닥)만 처리. **NYSE(`DNYS`)·AMEX 매핑 테이블로 확장 필수**(아니면 조용히 무데이터).
- ⚠️ **실계좌 연동 전 필드 인덱스(체결·호가 순서)를 KIS 최신 명세로 검증.** 현재 파서 값은 대표값. 국내/해외 순서가 다름.
- ⚠️ **해외 메시지 라우팅 키 검증** — 구독 키는 `trId|toUsKey(code)`인데 수신은 `trId|f[0]`. 해외 응답 `f[0]` 포맷이 다르면 핸들러 미발화. 검증 항목.
- 실전 `ws://ops.koreainvestment.com:21000` / 모의 `:31000`.

**[리뷰 결정] 구독 예산(subscription budget) 관리 — 한국 우선**
- 한투 웹소켓은 **세션당 실시간 등록 건수 제한(약 40여 건)** + **초당 20건** 제약. 관심종목 1개 = 체결+호가 2건.
- 규칙: **표시 중인 종목만 구독**(화면 밖 종목은 해제), 관심종목 상한 N개, 예산 초과 시 우선순위(포커스 종목 > 관심 리스트) 밀어내기.
- 한국 실시간 먼저 구현, **해외는 entitlement(유료/지연) 확인 후** 활성화.

**[리뷰 결정] approval_key 수명 관리 — 백엔드 갱신 + 재접속 시 키 재주입**
- `getKisSocket`은 최초 opt를 고정 → 짧게 만료되는 approval_key가 재접속 시 stale. **버그**.
- 수정: `opt.approvalKey`를 **getter/ref로 주입**해 `connect()`가 재접속 직전 최신 키를 다시 읽도록. 프론트는 만료 전 `/api/kis/approval` 재호출로 키 갱신.

---

## 1. 대시보드 (통합 시황 홈)

**목적**: 한 화면에서 시장 전체를 스캔. "오늘 시장 어때?"에 5초 안에 답.

**레이아웃**: 좌측(넓게) 3블록 세로 + 우측 4블록 세로.

### 좌측
| # | 컴포넌트 | 기능 | 데이터 |
|---|---|---|---|
| ① | 주요지수 카드 7개 | S&P·나스닥·다우·KOSPI·KOSDAQ·VIX·USD/KRW. 현재가·등락(값+%)·스파크라인 | 한투 REST + 지수 API, 폴링 |
| ② | **US 마켓 히트맵** (최대 강조) | Finviz식 트리맵. 섹터=열, 종목 타일=시총 비례 `flex-grow`, 색=등락%로 HSL 음영. **한눈에 크게** | S&P 구성종목 등락 |
| ③ | 실시간 관심종목 표 | 미국+한국 혼합. 시세 tick 시 flash 애니메이션. AI의견 배지. 행 클릭 → 종목상세 | 한투 WebSocket(체결) |

### 우측
| # | 컴포넌트 | 기능 | 데이터 |
|---|---|---|---|
| ④ | Fear & Greed 반원 게이지 | SVG 니들. 값 + Fear/Greed 라벨 + 1주/1달/1년 전 비교 | CNN F&G |
| ⑤ | 매크로 지수 리스트 | 전세자금·부동산·환율·금리·유가·금·BTC. 값+등락 | 각 지수/시세 API |
| ⑥ | AI 종합 투자의견 | 점수(0~100)·시장별 강도 바·호재/악재 근거 | 자체 AI(지수+뉴스+시세 종합) |
| ⑦ | 뉴스 헤드라인 피드 | 최신 헤드라인 요약(출처·시간·티커) | 뉴스 API/RSS |

**인터랙션/엣지**
- 히트맵 타일 hover: 종목명·등락·시총 툴팁. 클릭 → 종목상세.
- 관심종목 flash: 상승 tick=상승색 잔상, 하락=하락색. `prefers-reduced-motion` 존중.
- WebSocket 미연결 시: 관심종목·티커에 "연결 대기" 스켈레톤, 지수는 REST 폴링 값으로 폴백.
- 로딩/빈/에러 상태 각 블록 독립 처리(한 소스 실패가 화면 전체를 막지 않음).

---

## 2. 종목 상세

**목적**: 한 종목을 깊게. 실시간 시세 + 호가·체결 + AI 의견.
**레이아웃**: 3열 — 좌(관심종목 리스트) / 중(헤더+차트+호가·체결) / 우(AI의견+종목정보).
**참고 구현**: `handoff/StockDetail.tsx` (중·하단 영역 실동작 예시).

### 좌열 — 관심종목 리스트
- 종목 목록, 선택 종목 강조. 클릭 시 중앙/우측 갱신(선택 컨텍스트 = 소켓 구독 대상).

### 중열
| 영역 | 기능 | 데이터 |
|---|---|---|
| 헤더 | 종목명·코드·현재가·등락(실시간). tick flash | WebSocket `useKisTrade` |
| 52주 밴드바 | 현재가의 52주 최저~최고 위치 | REST |
| 차트 | 기간탭 1D/1W/1M/1Y. 면적차트(SVG gradient fill) | REST(과거) + 실시간 이어붙임 |
| 호가창 | 매도5·현재가·매수5. 잔량 depth 바 | WebSocket `useKisOrderbook`(H0STASP0/HDFSASP0) |
| 체결내역 | 시간·체결가·수량·매수/매도. 최근 N건 | WebSocket `useKisTrades`(H0STCNT0/HDFSCNT0) |

### 우열
| 영역 | 기능 | 데이터 |
|---|---|---|
| AI 투자의견 | 0~100 스코어 바 · 목표주가 · 상승여력 · 호재/악재 근거 | 자체 AI 스코어링 |
| 종목정보 | 시총·PER·PBR·EPS·배당·거래량 | 한투 REST |

**인터랙션/엣지**
- 종목 전환 시 이전 소켓 구독 해제 → 신규 구독(훅이 자동 처리, 마지막 구독자면 `tr_type:2` 해제).
- 해외 종목 소수 2자리, 국내 0자리(`dec` prop). 통화기호 `$`/`₩`.
- 장 마감/휴장: 실시간 대신 종가 표시 + "장 마감" 배지.
- 호가·체결 수신 전: "수신 대기…" 플레이스홀더.

---

## 3. 뉴스 (헤드라인 + 히스토리)

**목적**: 종목·시장 뉴스를 모아 저장하고 시간축으로 되돌아보기.
**레이아웃**: 2열 — 좌(필터) / 우(타임라인).

### 좌 — 필터
- 감성 필터: 전체/호재/악재/중립 (**실제 필터링 동작**).
- 출처별 카운트: CNN·Bloomberg·Reuters·연합인포맥스·CNBC·WSJ.
- 관심종목 태그 필터.

### 우 — 타임라인
- 날짜 그룹(오늘/어제/이번주) 헤더 **sticky**.
- 뉴스 카드: 출처 배지 · 감성 태그 · 시간 · 제목 · 요약 · 관련 티커 칩.
- **모든 헤드라인 자동 저장(히스토리)** — 과거 임의 날짜 조회 가능.

**데이터/파이프라인**
- 뉴스 API/RSS 수집 → DB 저장(중복 제거 = URL/제목 해시).
- 감성 분류: LLM(호재/악재/중립) + 종목 코드 매핑. 분류 결과도 저장(→ AI 의견 재료).

**엣지**
- 필터 결과 0건: 빈 상태 메시지.
- 티커 칩 클릭 → 해당 종목 상세로 이동.

---

## 4. 포트폴리오

**목적**: 내 계좌 손익을 미국+한국 통합(원화 환산)으로.
**레이아웃**: 상단 요약 카드 4개 + 하단 2열(보유 테이블 / 자산배분).

### 상단 요약 카드
총평가금액 · 평가손익(±값·±%) · 일간손익(±값·±%) · 투자원금.
- **미국주식은 USD/KRW로 환산해 원화 합산.**

### 하단
| 좌 | 우 |
|---|---|
| 보유종목 테이블: 종목·수량·평단·현재가·평가액·손익률·비중 바 | 자산배분 도넛(SVG) + 종목별 비중 범례 |

**데이터**
- 보유내역: 한투 계좌 REST.
- 현재가: WebSocket 실시간 → 평가액·손익 실시간 재계산.
- 환율: USD/KRW 실시간(환산 기준).

**엣지**
- 계좌 미연동(모의/게스트): 샘플 포트폴리오 또는 수동 입력 폴백.
- 손익 부호 색상 = 전역 컨벤션.

---

## 5. 리서치

**목적**: 종목별 AI 리포트를 읽는 화면.
**레이아웃**: 2열 — 좌(리포트 리스트, 선택형) / 우(AI 리포트 리더).

### 좌 — 리스트
종목 · 투자의견 배지 · 목표 상승여력 · 날짜. 클릭 시 우측 교체.

### 우 — 리더
- 헤더: 종목 · 의견 배지 · 신뢰도.
- 카드: 현재가 / 목표주가 / 상승여력.
- AI 종합 스코어 바.
- 투자 포인트 · 리스크 요인 (불릿).
- 밸류에이션: PER·PBR·시총·목표가.

**데이터/파이프라인**
- LLM 리포트 생성: 재무 + 뉴스 + 가격 → 요약/포인트/리스크. 캐싱(일 단위, 재생성 트리거 별도).

---

## 6. 구현 우선순위 (엔지니어링 리뷰 반영)

**리뷰 결정**: 백엔드를 M1 안에 섞지 않고 **M0으로 먼저 분리**한다(크로스모델 합의). 백엔드가 없으면 M1도 M3도 완성 불가 → 중반 재작업을 막기 위함.

| 단계 | 범위 | 이유 |
|---|---|---|
| **M0** | **백엔드 기초** — 프록시 서버, DB 스키마(가격 시계열·뉴스·캐시·포트폴리오 스냅샷), approval_key 발급/갱신, 캐시 TTL 정책, 레이트리밋 흡수 | 모든 화면의 토대. 여기부터 시작 |
| **M1** | 대시보드 지수 통합 뷰(①②④⑤) + 지수 히스토리 저장 | 인증·소켓 없이 가장 빨리 "매일 여는 화면" 완성 |
| **M2** | 뉴스 수집·저장·감성분류 + 뉴스 화면 | 히스토리·AI의견의 재료 축적 시작 |
| **M3** | 한투 인증(approval_key 백엔드) + WebSocket 실시간(관심종목③·종목상세 호가·체결) | 가장 어려움. **착수 조건**: 필드 인덱스 검증 + 해외 entitlement 확인 |
| **M4** | AI 투자의견·리서치(⑥·리서치) + 의견 히스토리 아카이브 | M0~M3 데이터가 있어야 성립. **비용 모델 선결** |
| **M5** | 포트폴리오(계좌 REST 연동) | 실계좌 연동, 별도 승인 필요 |

---

## 7. 테스트 계획 (리뷰 결정: 전체 커버리지 목표)

파서 필드 인덱스는 **계약(contract)** 이라 조용히 틀림 → 골든 픽스처로 잠근다.

```
useKisSocket.ts
  ├── parseTrade — 골든 프레임(국내/해외) → Trade 매핑
  ├── parseOrderbook — 5호가 매핑 + reverse
  ├── subscribe — refcount 중복구독 dedup / 마지막 해제 시 tr_type:2
  ├── 재접속 backoff (1s→2s→…→30s 상한)
  ├── onMessage — PINGPONG 반사 / 라우팅 키 매칭
  ├── approval_key 재주입 — 재접속 시 최신 키 사용 (회귀)
  └── toUsKey — 거래소별 프리픽스 (회귀)
StockDetail.tsx
  ├── 호가 depth 바 폭 계산 (maxQty=0 방어)
  └── 종목 전환 시 이전 구독 해제  [→E2E]
```
- 러너: Vitest(단위) + Playwright(E2E). WebSocket은 mock 서버로 프레임 주입.
- **골든 픽스처는 KIS 실명세로 한 번 맞춘 뒤 회귀로 고정.**

## 8. 미흡한 점(Gap) — 명세 보강 (리뷰 반영)

1. **백엔드 명세** → M0으로 분리(§6). DB 스키마·API 계약·토큰 갱신·캐시 무효화 포함.
2. **데이터 소스 합법성/entitlement** — CNN F&G 무료 공식 API 사실상 없음, Finviz 스크래핑=TOS 위반. **히트맵은 자체 구성**(S&P 구성종목+시총+등락 직접 수집), F&G는 대체 소스/자체 산출 검토. M1 착수 전 소스별 무료/유료·합법 경계 확정.
3. **멀티소스 정합성** — KIS 실시간 vs 백엔드 캐시 vs AI 캐시 충돌 시 원본 우선순위 규칙 정의. "몇 분 전" staleness 뱃지 표시.
4. **캐시 TTL** — 지수 스냅샷 주기, AI 리포트 캐시 수명, 장 마감 후 폐기 규칙. 미정 시 어제 종가로 판단하는 사고.
5. **타임존/장 운영시간** — 서울(UTC+9)·미국(EST/EDT, 서머타임)·뉴스 다국 시간대. "오늘 고가" 리셋 기준, 프리/애프터마켓 표시.
6. **AI 비용/리스크** — 모델 선택·쿼리 비용·캐시로 비용 억제·환각 면책(사내 사용 시 liability). M4 선결.
7. **포트폴리오 정합성** — 다통화 환율 기준(실시간/마감), 분할·배당·권리락, KIS 계좌 15분 지연을 "지연" 명시.
8. **인증/계정 정책** — 개인용(로컬/사설) vs 사내(SSO). 배포 형태를 좌우.
9. **화면별 상태** — 로딩/빈/에러를 화면별로 구체화.

## 9-1. 다음 스텝 (2026-07-29 세션 랩업 — 우선순위 확정)

> 대시보드 코어(실데이터·실시간·순위·목표가·RADIO 규약)는 완료. 아래는 다음 세션 착수 순서.

1. **URL state** (`?tab&code`) — 새로고침/공유 시 대시보드로 리셋되는 RADIO 갭. 선행 없음.
2. **git init + 최초 커밋** — 저장소 미초기화 상태. 수정별 커밋·리뷰 흐름의 전제.
3. **web-vitals 관측성** — LCP/INP/CLS p75 + time-to-first-tick을 sendBeacon 채널(`/api/stream/bye` 패턴 재활용)로 수집. RADIO 갭.
4. **Finnhub 애널리스트 컨센서스** — 무료 확인됨(`/stock/recommendation`). US 관심종목 AI 시그널을 모멘텀→실제 컨센서스로 교체.
5. **KIS 실전 계좌 전환 계획** — 분봉 실데이터(1일 차트)·SSE 등락률 필드·VTS 합성가 해소·파서 골든 픽스처 갱신을 한 번에.
6. **디자인 토큰 통합 리팩터** — 3개 스타일 시스템 공존·타이포 18단계 정리. ⚠️ 부동산 스크리너 세션 랜딩 후(충돌 방지).
- 보류: 나스닥 순위(유료 소스 — 사용자 "나중에"), S11 숨김탭 SSE(모바일 로드맵 확정 시).

## 9. 열린 결정 항목

1. **뉴스 API 선택** — 유료(커버리지↑) vs RSS 조합(무료·불안정). M2 착수 전 결정.
2. **AI 스코어링 방식** — 규칙 기반 가중합 vs LLM 판단 vs 하이브리드. M4 전 프로토타입.
3. **한투 실전/모의 계좌** — 초기 개발은 모의(:31000)로. 실전 전환 시점·계좌 준비.
4. **히트맵 범위** — S&P500만 vs 한국 대형주 병행. M1은 US만 권장(데이터 단순).
5. **배포/호스팅** — 개인용(로컬/사설) vs 사내(인트라). 인증·계정 정책이 달라짐.

---

## Implementation Tasks
이번 리뷰 발견에서 도출. Claude Code/Codex로 실행, 완료 시 체크.

- [ ] **T1 (P1)** — backend — approval_key getter/ref 주입 + 재접속 시 최신 키 재사용
  - 출처: 아키텍처 — useKisSocket.ts:161-165 싱글턴 staleness
  - Files: `handoff/useKisSocket.ts`, 백엔드 `/api/kis/approval`
  - Verify: 키 만료 후 재접속 시 재구독 성공하는 단위테스트
- [ ] **T2 (P1)** — realtime — 구독 예산 관리(표시 종목만 구독, 상한 N, 초당 20건)
  - 출처: 아키텍처 — KIS 세션당 등록 한도 + 20 req/s
  - Files: `handoff/useKisSocket.ts`(예산 레이어), 대시보드 관심종목
  - Verify: 상한 초과 시 우선순위 밀어내기 테스트
- [ ] **T3 (P1)** — realtime — `toUsKey` 거래소 매핑 테이블(NYSE/AMEX) + 라우팅 키 검증
  - 출처: 코드품질 — useKisSocket.ts:122 / :92
  - Files: `handoff/useKisSocket.ts`
  - Verify: NYSE 종목 골든 프레임 → 핸들러 발화 테스트
- [ ] **T4 (P1)** — tests — 파서 골든 픽스처 + 핵심 로직 Vitest
  - 출처: 테스트 — 커버리지 0%
  - Files: `tests/useKisSocket.test.ts`
  - Verify: `npx vitest run`
- [ ] **T5 (P1)** — backend — M0 백엔드 기초(프록시·DB 스키마·캐시 TTL)
  - 출처: 크로스모델 합의 — 백엔드 명세 부재
  - Files: 신규 `server/`
  - Verify: 지수 프록시 엔드포인트 응답 + 스냅샷 적재
- [ ] **T6 (P2)** — data — 데이터 소스 합법성/entitlement 확정(F&G·히트맵·해외 실시간)
  - 출처: 아웃사이드 보이스 — TOS/유료 경계
  - Verify: 소스별 무료/유료·합법 경계 문서화
- [ ] **T7 (P2)** — frontend — 멀티소스 정합성 규칙 + staleness 뱃지
  - 출처: 아웃사이드 보이스 — 값 충돌 시 신뢰 상실
  - Files: zustand 스토어, 공통 표시 컴포넌트

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | issues_open | 10 issues, 1 critical gap |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **CROSS-MODEL:** Claude 리뷰 + 서브에이전트 아웃사이드 보이스 모두 "백엔드 명세 부재"를 최상위로 지목 → M0 분리로 반영. 서브에이전트 신규 발견(데이터 합법성·캐시 TTL·멀티소스 정합성·AI 비용·포트폴리오 정합성)은 §8에 흡수.
- **VERDICT:** ENG REVIEW 완료 — 4개 결정 반영(M0 분리 / 구독 예산·한국 우선 / approval_key 재주입 / 전체 테스트 커버리지). 구현 착수 가능.

**UNRESOLVED DECISIONS:**
- §9 열린 항목(뉴스 API, AI 스코어링 방식, 실전/모의 계좌, 히트맵 범위, 배포 형태)은 각 마일스톤 착수 전 확정 필요 — 지금은 미결.
